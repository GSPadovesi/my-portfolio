export interface TrajectoryProps {
  id: string;
}

export interface experienceinfoprops {
  id: number;
  label: string;
  icon: string
}

export interface SkillInfo {
  id: number;
  label: string;
  icon: string;
  color: string;
}